# 15 二开冲刺规划（一周）

本文是给后续 Agent 直接接手用的。每次换窗口先读第 0 节，再决定从哪一天/哪个模块往下推。
不写代码层细节，只到业务规则与里程碑层。状态更新只改第 0 节，其它章节不要改，除非用户明确要求。

---

## 0 给后续 Agent 的「快速接手」段（每次工作完更新这一节）

当前阶段：Day 7 已收口，V1 交付物已齐；另在主线之外已落地「演示库 Text2SQL 入参平台门闸」（不计入原一周 Day 1–7 里程口径）。
最近一次产出：`apps/common/utils/demo_sql_gate.py` + `apps/common/utils/tool_code.py`（`ToolExecutor.exec_code` 前统一处理 `sql`：Markdown 剥离；`database=after_sales_demo` 时与演示工具一致的只读白名单校验）；`mock_data/text2sql/README.md` 补充说明；前端二开品牌化、`LogoFull` DOM 化修复中文问号、登录左栏 `ui/public/theme/default.jpg` 替换；全局 Skills 自 `C:\Users\tlx\.cursor\skills` 同步至 WSL `~/.cursor/skills`。交接与对话摘要见 `10_交接文件.md` §6.5。
当前进行中：无（待用户在运行实例上重启 Web 后验收 SQL 门闸与原有 Text2SQL 工作流）。
下一动作：重启 Web 后各走一条「合法查询（含 Markdown 代码围栏包裹的 SQL）」与「违规 SQL」验收；若继续 Text2SQL 二开，再定是否做「门闸与 safe_pg 粘贴版规则自动同步」或 judge/292/UI 重导等项。

已锁定的关键决策：
- 主线 = 基于 MaxKB 的 3C 数码售后场景领域化 RAG 二开，强调平台层二开价值
- baseline 与最终版尽量使用同一套知识库和同一套题集，方便清晰体现前后差距
- 数据集准备由用户主导；AI 的重点是规则设计、代码改造、评测闭环和文档整理
- 知识库主格式优先用 Markdown；题集使用结构化 JSON
- 主体价值点 = 领域化 RAG 全链路优化 + 售后合规 / 风险控制
- 扩展项 = 产品型号-规则自动匹配、轻量槽位继承、售后工单自动生成、外部工具查询
- 一周内出 V1 可演示版本
- 直接在主仓当前分支以新功能模块形式做改造，不做插件化
- Text2SQL：原规划为「仅维护现状、不进一周主线」；现已增加「演示库入参平台门闸」作为小步源码级加固（见 `10_交接文件` §6.5），与画布拼装并行；更大范围（多库策略、审计表、与对话管道深度编排）仍属后续
- 文档与对话停留在业务/概念层，不下钻代码层
- 当前桌面知识库目录：`/mnt/c/Users/tlx/Desktop/3C售后知识库/`；5 大类为保修政策、退换货规则、售后 SOP、FAQ、产品手册
- 当前桌面题集目录：`/mnt/c/Users/tlx/Desktop/3C售后题集/`；合并结果文件包括 `cases_300_中文审核版.json` 与 `cases_300_评测脚本版.json`
- 题集当前可先用于 baseline 摸底，但评测脚本版 JSON 的关键词规则存在质量风险：部分 `answer_contains_*` / `answer_not_contains` 写得偏说明文字而非机器可判定关键词；不必整套重写，若 baseline 结果异常，优先修高风险题、应拒答题、SOP 题的规则层
- 本地 MaxKB 当前向量存储走本机 Postgres + pgvector；知识库上传后切分与向量化结果存本地数据库，不在云端
- 本地仓库默认指向本地 embedding 模型 `shibing624_text2vec-base-chinese`，但本窗口未确认本地模型服务一定已就绪；baseline 更建议先用用户当前稳定可用的云端 embedding 模型，避免引入额外变量

---

## 1 项目主线与岗位画像

主线一句话：以 3C 数码售后为入口，对 MaxKB 的知识检索链路做强领域化二次开发，核心做分块、检索/轻量重排、弱证据拒答和高风险话术约束，再用同一套知识库与题集做改造前后对照实验。

岗位画像：LLM 应用 + RAG 平台中间地带。要既能讲业务怎么落地（售后规则、客服风险、场景边界），又能说清平台层为什么这样改、改了什么、带来了什么差值。

本周主线里的核心价值排序：
1. 3C 售后场景的领域化 RAG 全链路优化
2. 售后合规与风险控制
3. 产品型号-规则自动匹配（作为强相关扩展）

不在主线内的事（明确不做，避免后续抢节奏）：
- Text2SQL 的源码级深度改造
- 多语言支持
- 重排模型微调
- 完整知识图谱 / Neo4j（路线图项）
- 大而全的记忆管理系统
- 只为了追求大样本而扩文档 / 扩题量

---

## 2 一周里程碑

每天按 4 栏组织。前两栏是你自己要扛的，后两栏是给 AI 的活和当天验收。
"今天怎么算完成"是判定收工的硬条件，不是"做了多久"。

### Day 1 周一：字段骨架对齐 + 场景边界锁定

业务/概念你要先搞懂：
- 这次项目的价值不在"做一套问答数据"，而在"把通用 RAG 平台改造成更适合 3C 售后的系统"。
- 3C 售后最关键的通用字段骨架：品牌、品类、型号、SKU/版本、SN/IMEI、购买时间、激活时间、保修期、故障现象、责任判定、售后诉求、处理方式、配件状态。
- 字段骨架大体通用，但具体规则值不通用；不同品牌会在保修时长、拒保条件、维修方式、发票要求上不同。
- 为什么一定要固定同一套知识库和题集做 baseline 与最终版：这样对照实验最干净，二开价值最容易被看见。
- 面试常见追问：为什么不是先去做更多数据。回答口径：当前核心是验证平台改造是否有效，先把变量控制住比盲目扩样本更重要。

你要自己做的：
- 确认一套可长期复用的 3C 知识库与题集口径，不再按"第一版 / 最终版"切两套。
- 确认文档主格式统一为 Markdown，题集主格式统一为 JSON。
- 对已有或准备中的数据集做一次人工抽样，确认至少覆盖品牌、型号、故障、保修 / 拒保、SOP、FAQ 这些骨架。

你要在画布上亲自点的：
- 暂不动。

你交给 AI 做的：
- 把 3C 的字段骨架、文档类型、评测口径写进规划与相关文档。

今天怎么算完成：
- 第 0 节写清"同一套知识库 + 同一套题集"的对照实验口径。
- 你和 AI 对 3C 通用字段骨架达成一致。

### Day 2 周二：知识库结构定型 + 题集结构定型

业务/概念你要先搞懂：
- 知识库是"教材"，题集是"考卷"；二者必须对应，但不能混用。
- 知识库最重要的是结构清晰、字段齐、文档类型齐，不是先追求数量。
- 题集至少要覆盖三类：应回答、应拒答、边界题；否则无法证明拒答与合规机制有效。
- 3C 文档里高价值的类型通常包括：品牌 / 型号资料、保修与拒保政策、退换货规则、排障 SOP、维修 / 服务渠道规则、FAQ / 高风险问答。
- 面试常见追问：为什么选 Markdown。回答口径：层级清晰、便于分块、便于保留规则条款和表格语义，最适合 RAG 导入。

你要自己做的：
- 准备或整理你要长期复用的知识库文档与题集。
- 确认文件命名和目录约定，避免后续做差异化分块时识别不出类型。

你要在画布上亲自点的：
- 暂不动。

你交给 AI 做的：
- 整理 `docs/after_sales_assistant/16_数据集说明.md`：写清文档格式、目录分布、题型结构、基线与最终版共用同一套数据的原则。
- 如有需要，补一份知识库目录命名建议与文档字段建议。

今天怎么算完成：
- 知识库与题集的结构已经固定，不再摇摆。
- `16_数据集说明.md` 存在并能说明数据口径。

### Day 3 周三：baseline 调参与评测扩展

业务/概念你要先搞懂：
- baseline 的意义是拿到一把尺子，而不是证明当前系统已经很好。
- LLM-as-judge 是补语义层评分；关键词规则是补可复查性，两者一起用更稳。
- 高风险问题不能只看"答得像不像"，还要看"有没有越权承诺、有没有乱判责"。
- 检索三个核心参数业务上的意思：top_n 是召回多少条片段，相似度阈值是低于此值就丢，引文长度是给模型多少上下文。
- 面试常见追问：为什么 judge 不能和回答模型同源。回答口径：避免同模型自评偏差。

你要在画布上亲自点的：
- 上传你的 3C 知识库到 MaxKB，新建一个 3C 售后知识库。
- 配置售后助手应用切到这个知识库，跑通一次端到端问答。
- 在知识库检索节点做参数调试，锁定 baseline 参数组。

你交给 AI 做的：
- 扩展评测脚本，输出关键词通过率、LLM-as-judge 平均分、拒答正确率、高风险合规通过率。
- 跑 2 到 3 组参数报告，帮助锁定 baseline。
- 产出第一版 baseline 报告。

今天怎么算完成：
- baseline 报告存在。
- 第 0 节里写明 baseline 的核心指标与参数组。

### Day 4 周四：分块策略二开（核心一）

业务/概念你要先搞懂：
- 分块是离线动作，决定后续检索能不能召回到正确规则。
- 3C 售后文档高度依赖字段和规则边界，如果切错，会直接导致错型号、错判责、错保修。
- 不同文档类型必须用不同切分单元；不能再用统一通用分块吃掉所有 3C 文档。
- 通用 fallback：未识别为 3C 售后类型的文档继续走 MaxKB 原分块器。
- 面试常见追问：为什么不用模型自动判断分块边界。回答口径：分块是规则性很强的离线动作，规则化更稳定可控。

你要在画布上亲自点的：
- 等 AI 改完后，去 3C 知识库把文档删掉重传，确认开启售后模式后分段数量和边界发生变化。

你交给 AI 做的：
- 在 MaxKB 后端导入分块入口加"售后模式"分支，按第 3.1 节规则做差异化切分。
- 在导入接口暴露开关字段，供前端 Day 6 使用。

今天怎么算完成：
- 按售后模式重导入后，抽查至少 5 篇文档，分段边界符合 3.1 节规则。
- 分块结果能明显体现 3C 文档类型差异。

### Day 5 周五：多路召回 + 弱证据拒答 + 合规约束

业务/概念你要先搞懂：
- 3C 售后场景里，错误承诺和错误判责是高风险点，风险控制比"回答更顺"更重要。
- 仅靠 prompt 不能彻底解决幻觉，必须在系统层做拦截。
- 多路召回不只是向量相似度，还要吃进品牌、型号、SKU/版本、SN/IMEI、故障现象等结构化信息。
- 弱证据拒答 = 证据不足时宁可不答，也不允许模型替你拍板保修、赔付、时效承诺。
- 面试常见追问：为什么拒答不会影响体验。回答口径：在高风险场景里，可控的拒答比错误承诺更符合业务价值。

你要在画布上亲自点的：
- 编几条高风险问题人工测试，例如"进液后还能免费保修吗"、"没有发票能直接换新吗"、"这个型号是不是一定属于质量问题"。
- 检查回答节点提示词，确认固定拒答模板和高风险约束没有被模型重写。

你交给 AI 做的：
- 在检索链路加多路召回（向量 + 关键词 + 元数据加权）。
- 加弱证据拒答判定与高风险问题合规约束。
- 评测脚本输出"拒答正确率"与"高风险合规通过率"。

今天怎么算完成：
- 高风险问题能稳定走固定模板或澄清逻辑。
- 报告中有拒答正确率与高风险合规通过率。

### Day 6 周六：轻量重排 + 型号规则匹配 + 配置开关 + 对照实验

这一天负担最重，前面 5 天必须按时收尾才能进入。

业务/概念你要先搞懂：
- 轻量重排的意义是把最相关的规则排到更前面，而不是引入复杂模型堆效果。
- 型号-规则自动匹配是最贴近 3C 业务的扩展能力，能直接减少错召回和错规则。
- 配置面"默认关闭"的产品化思考：保证存量用户行为不变，新功能必须显式开启，避免误伤老用户。
- 控制变量：对照实验里只能让一个东西变（这里是售后模式开关），其它都固定（知识库、题集、提示词、模型、检索参数）。
- 面试常见追问：为什么选型号匹配而不是直接上更多工具。回答口径：型号匹配与核心检索链路强相关，最能体现 3C 领域化价值。

你要在画布上亲自点的：
- 进入 3C 知识库设置，切换"售后模式"开关，验证开关开 / 关时行为不同。
- 选几条带型号的问题，复跑开 / 关两种模式，确认召回与排序变化。

你交给 AI 做的：
- 在召回结果合并后加轻量重排。
- 加入产品型号-规则自动匹配逻辑（至少做到识别型号并参与过滤或加权）。
- 前端在知识库设置加"售后模式"开关，默认关闭，状态持久化。
- 跑两轮对照实验：售后模式 OFF 与 ON。
- 写 `docs/after_sales_assistant/17_对照实验报告.md`，对比关键词通过率、LLM 平均分、拒答正确率、高风险合规通过率、平均耗时、失败题分布。

今天怎么算完成：
- 知识库设置页能看到"售后模式"开关，能开能关、刷新后保持。
- 对照实验报告存在，且能看出改造前后差异。

### Day 7 周日：项目总结 + 简历包装

业务/概念你要先搞懂：
- 简历讲故事的三段式：背景 → 改造 → 结果。背景说"通用 RAG 在 3C 售后为什么不够"，改造说"做了哪些领域化设计"，结果说"对照实验下的多指标差值"。
- 反向追问的常见角度：为什么一定要做拒答、为什么型号匹配重要、为什么同一套题集更合理、为什么没有先堆大样本。
- 自我介绍口径：以"二开 MaxKB 的 3C 数码售后场景 RAG 链路"为引子，重点放在"为什么这样改"，不要一上来讲技术细节。

你要在画布上亲自点的：
- 录一段开关 OFF vs ON 的演示。
- 把对照实验报告关键表格截图一张。

你交给 AI 做的：
- 起草 `docs/after_sales_assistant/18_项目说明_面试版.md`：项目背景、改造内容、指标结果、典型反向追问拟答。
- 按本文第 6 节口径生成 1 段总结 + 4 个 bullet 的简历段。
- 整理项目目录结构图（高层描述，不下钻代码）。

今天怎么算完成：
- `18_项目说明_面试版.md` 存在。
- 简历段可以直接复用。
- 屏幕录像 / 截图素材已留档。

---

## 3 模块业务规则（写死，后续 Agent 直接按这些规则做）

### 3.1 售后模式分块规则（3C 6 类）

按文档类型走不同切分单元，文档类型由文件命名约定或人工标记决定，不依赖模型自动分类。

- 品牌 / 型号资料类：每个型号或版本一个单元，保留品牌、型号、SKU/版本、适用品类、关键差异
- 保修与拒保政策类：每条保修 / 拒保规则一个单元，保留保修期限、适用条件、拒保情形、判责依据
- 退换货规则类：每条退 / 换 / 退款规则一个单元，保留时效、凭证要求、配件要求、渠道限制
- 故障诊断 / 排障 SOP 类：每个故障现象或流程步骤组一个单元，保留前置条件、排查顺序、升级条件
- 维修 / 服务渠道规则类：每个服务条款一个单元，保留维修方式、寄修 / 到店 / 授权点、时效、费用说明
- FAQ / 高风险问答类：每问一个单元，保留允许直接回答的边界与需补充字段
- 通用 fallback：未识别为售后类型的文档继续走 MaxKB 原分块

每种类型保留两条公共规则：
- 不切断列表项、表格行与关键条件组合
- 长度上限可超出原默认值，宁愿单元偏大，也不把规则条件切散

### 3.2 检索 / 召回规则

- 主路：向量召回
- 副路：品牌、型号、SKU/版本、SN/IMEI、故障现象、配件名的关键词精确召回
- 元数据加权：售后模式下，按问句中识别到的品牌、品类、型号、文档类型对相应单元加权
- 型号优先：问句中出现明确型号时，优先过滤或提升对应型号文档
- 召回上限不放太大，宁可严格也不要把无关分段挤进来

### 3.3 弱证据拒答与合规规则

满足以下任一条件就走拒答或澄清：
- 召回最高相似度低于阈值
- 召回条数低于最小阈值
- 主关键词（品牌、型号、SKU/版本、SN/IMEI）在召回单元里完全不出现
- 用户询问保修资格、责任判定、维修费用、时效承诺，但知识库未提供明确规则依据
- 问句中含有具体型号或序列字段，但知识库没有对应条目

高风险问题固定约束：
- 不允许在证据不足时直接承诺"一定保修 / 一定换新 / 一定免费维修"
- 不允许在证据不足时直接判定"属于质量问题 / 属于人为损坏"
- 不允许在证据不足时直接承诺具体维修费用与完成时效

拒答固定模板（不让模型重写，每次只填中括号变量）：

当前知识库未提供足够依据来回答 [简短复述用户问题]。建议补充品牌、型号、购买 / 激活信息或故障细节后再问；如需进一步处理，请走对应品牌官方售后渠道或人工客服。

### 3.4 配置面（前端）

- 入口：知识库设置（也可以是创建知识库时）
- 控件：单一开关 `售后模式`
- 默认值：关闭
- 开启后：导入分块走第 3.1 节，检索走第 3.2 节，拒答与合规约束走第 3.3 节
- 关闭后：完全等同于原 MaxKB 行为
- 切换不影响已经导入的内容，提示"开关切换仅对此后导入的内容生效"

### 3.5 评测扩展（双指标 + 拒答 + 合规）

- baseline 与最终版必须使用同一套知识库和同一套题集
- 指标 1：关键词子串通过率（保留旧规则）
- 指标 2：LLM-as-judge 平均分（0-2 分），judge 模型必须与回答模型不同源
- 指标 3：拒答正确率（仅在应拒答用例上统计）
- 指标 4：高风险合规通过率（仅在保修资格 / 判责 / 费用 / 时效承诺等高风险问题上统计）
- 报告里至少并列展示 1、2、3、4 四项；面试讲"双指标对照"时主指 1 和 2，3 和 4 作为业务风险价值补充
- judge prompt 写到脚本配置文件里可审阅，不允许动态生成

---

## 4 样本节奏（不要在 V1 阶段越线）

- V0：早期小样本验证（5 篇 + 30 题，已归档）
- V1 数据准备（Day 1 - Day 2）：由用户锁定一套 3C 知识库与题集，baseline 与最终版共用
- V1 改造期（Day 3 - Day 6）：在这同一套知识库与题集上完成全部改造与对照实验
- 路线图（不进本周）：再扩更多品牌、更多品类、更大题量

为什么这里不把文档数和题量写死：当前最关键的是控制变量、把二开价值讲清楚，而不是为了好看去追数字。数量够用即可，结构必须齐。

---

## 5 改造前后对照实验设计

固定输入：同一套 3C 知识库 + 同一套题集（含应回答、应拒答、边界题、高风险题）

对照两组：
- 改造前 baseline = Day 3 锁定的那份成绩，售后模式开关关闭
- 改造后 = 售后模式开关开启，同一套知识库重新按售后模式导入

6 行对比：
- 关键词通过率
- LLM-as-judge 平均分
- 拒答正确率
- 高风险合规通过率
- 平均耗时
- 失败题分类分布（按文档类型 + 路由错 + 型号错召回）

输出形式：一张 6 行 2 列的小表 + 一段不超过 300 字的归因说明。这张表和这段归因，就是面试 PPT 里能直接放上去的核心结论。

---

## 6 简历包装方向

定位口径：基于 MaxKB 的 3C 数码售后场景领域化 RAG 二次开发。不要写成"二次开发了一个聊天机器人"，写法要让平台层改造和业务风险控制部分被看见。

简历段落建议（1 段总结 + 4 个 bullet）：

总结：基于 MaxKB 开源平台，针对 3C 数码售后场景做了知识检索链路的领域化二次开发，落地"售后模式"开关，包括按文档类型的差异化分块、多路召回与轻量重排、弱证据拒答与高风险话术约束，并在同一套知识库与题集上完成改造前后对照实验。

bullet 框架（具体数字按第 5 节实测填）：
- 在 MaxKB 主仓新增 `售后模式` 功能模块，针对品牌 / 型号资料、保修与拒保、退换货、排障 SOP、维修渠道、FAQ 六类 3C 售后文档分别采用差异化分块策略
- 检索侧加入品牌、型号、SKU/版本、SN/IMEI、故障现象等结构化关键词召回，并结合文档类型元数据加权与轻量重排，提升 3C 场景的召回稳定性
- 引入弱证据拒答与高风险问题合规约束机制，降低模型在保修资格、责任判定、维修费用和时效承诺场景下的错误回答风险
- 在同一套 3C 知识库与题集上完成改造前后多指标对照，关键词通过率从 X% 提升至 Y%，LLM-as-judge 平均分从 X 提升至 Y，高风险合规通过率达到 X%

---

## 7 不在本周做的事（后续 Agent 不要被用户临时要求带偏）

如果用户在本周内提出以下任意一项，先反问"这条要不要从一周计划里删一个等价工时的事来换"，不要默默接活：

- 为了好看继续无上限扩文档 / 扩题量
- 重排模型微调或更换嵌入模型
- 多语言支持
- Neo4j 知识图谱
- Text2SQL 链路源码级改造
- 把售后模式做成跨知识库的全局开关（V1 限定为单知识库设置）
- 大而全的长期记忆系统
- 快递 / 网点 / 外部工具深度对接
- 售后工单自动生成的完整闭环系统

---

## 8 状态更新约定

每个 Agent 工作完一轮，只改第 0 节这 5 行：
- 当前阶段：例如 `Day 2 进行中`
- 最近一次产出：写最近落地的文件路径
- 当前进行中：写正在做的具体事
- 下一动作：写下一个 Agent 接手时第一件要做的事
- 已锁定的关键决策：只在用户明确改了主线时更新

第 1-7 节不要改。如果发现规则里有不合理处，先在第 0 节"下一动作"里写"建议用户确认是否调整 X.X 节"，等用户拍板再改。
