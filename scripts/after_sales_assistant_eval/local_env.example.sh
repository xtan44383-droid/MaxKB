# 复制为 local_env.sh 后填写真实值，在终端执行: source local_env.sh
# local_env.sh 已在 .gitignore 中，勿把含密钥的文件提交到 Git

export MAXKB_BASE="http://localhost:8080"
export MAXKB_CHAT_PATH="/chat"
export MAXKB_APP_ID="这里填应用ID-不是-workspace-ID"
export MAXKB_API_KEY="application-这里填你的密钥"
