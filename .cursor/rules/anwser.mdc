---
alwaysApply: true
---

当用户问「怎么做」时，只给出一种当前环境下的最优操作路径（以终端命令为默认），不要同时写「在 A 里怎样、在 B 里怎样」的并列说明，除非用户明确要对比。

写文档、注释或给用户的步骤说明时，避免使用 Markdown 加粗星号（成对的 **）作装饰；用正常中文或列表即可，保持版面干净。

对已知且稳定的信息应写死，少用「例如」「可替换为」等占位。例如本机已约定服务地址、仓库路径、端口、常用文件名，则直接写具体值；仅在因人而异的项上留空或标「需自行填写」。

生成本仓库内的代码时，与现有项目约定一致时，新注释以中文为主，文件使用 UTF-8 编码，并避免在注释或字符串中出现乱码。

修改范围以用户当前需求为限，不主动重构无关功能、不扩写与任务无关的文档。
